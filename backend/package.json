{
  "name": "study-app-backend",
  "version": "1.0.0",
  "private": true,
  "description": "StudyApp MVP API and production web server",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}

